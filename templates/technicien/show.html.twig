{% extends 'base.html.twig' %}

{% block title %}Technicien{% endblock %}

{% block body %}
    <h1>Technicien</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ technicien.id }}</td>
            </tr>
            <tr>
                <th>NomTechnicien</th>
                <td>{{ technicien.nomTechnicien }}</td>
            </tr>
            <tr>
                <th>PrenomTechnicien</th>
                <td>{{ technicien.prenomTechnicien }}</td>
            </tr>
            <tr>
                <th>QualifTechnicien</th>
                <td>{{ technicien.qualifTechnicien }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_technicien_index') }}">back to list</a>

    <a href="{{ path('app_technicien_edit', {'id': technicien.id}) }}">edit</a>

    {{ include('technicien/_delete_form.html.twig') }}
{% endblock %}
